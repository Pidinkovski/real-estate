/** Shown in the UI (footer, CTA mailto). Set `NEXT_PUBLIC_CONTACT_EMAIL` to override. */
export const SITE_CONTACT_EMAIL =
  process.env.NEXT_PUBLIC_CONTACT_EMAIL?.trim() || 'info@virtusdecora.com';
